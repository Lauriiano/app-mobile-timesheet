export type Colors = {
    primary: string;
    primaryLight: string;
    secondary: string;
    background: string;
    error: string;
};

export type FontSizes = {
    small: number;
    medium: number;
    large: number;
    letterSpacing: number;
};
